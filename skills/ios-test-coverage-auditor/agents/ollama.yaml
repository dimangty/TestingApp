interface:
  display_name: "iOS Test Coverage Auditor"
  short_description: "Find missing test coverage and produce an actionable backlog"
  default_prompt: "Build mode. Highest priority rule: when user says 'Execute exactly one command', run only that command and stop. Otherwise map coverage and return actionable backlog. No planning loops or pseudo tool tags."
