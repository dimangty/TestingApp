interface:
  display_name: "iOS Selected Presenter SwiftTesting Writer"
  short_description: "Cover one presenter with Swift Testing and SwiftyMocky"
  default_prompt: "Build mode. Use $ios-selected-presenter-swifttesting-writer to write tests for one presenter and persist file edits. If presenter and output path are given, execute directly without questions or preamble. Do not call todolist/todowrite/skill, do not emit <function=...> tags, and fallback to bash if write fails."
